---
## Default Logstash configuration from Logstash base image.
## https://github.com/elastic/logstash/blob/main/docker/data/logstash/config/logstash-full.yml
#
http.host: 0.0.0.0

node.name: logstash
# pipeline.id: system_logs_pipeline
# path.config: "/usr/share/logstash/pipeline/systemlog.conf"

# pipeline.id: user_logs_pipeline
# path.config: "/usr/share/logstash/pipeline/userlog.conf"
